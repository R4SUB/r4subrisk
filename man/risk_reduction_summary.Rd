% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_reduction.R
\name{risk_reduction_summary}
\alias{risk_reduction_summary}
\title{Summarize Risk Reduction Between Two Registers}
\usage{
risk_reduction_summary(before, after, config = risk_config_default())
}
\arguments{
\item{before}{A \code{risk_register} (earlier snapshot).}

\item{after}{A \code{risk_register} (later snapshot, usually the result of
\code{\link[=apply_mitigations]{apply_mitigations()}}).}

\item{config}{A \code{risk_config} from \code{\link[=risk_config_default]{risk_config_default()}}, used to order the
risk-level bands when deciding what counts as a downgrade.}
}
\value{
A list of class \code{"risk_reduction"} with overall figures and a
\code{per_risk} tibble of the common risks. Fields include \code{total_rpn_before},
\code{total_rpn_after}, \code{total_rpn_reduction}, \code{pct_reduction},
\code{mean_rpn_before}, \code{mean_rpn_after}, \code{n_reduced}, \code{n_increased},
\code{n_unchanged}, \code{n_downgraded}, \code{n_added}, \code{n_resolved}, and \code{per_risk}.
}
\description{
Quantifies how much risk was removed between an earlier and a later risk
register, so a mitigation effort can be reported as a single before/after
story. Regulatory reviewers expect to see that identified risks were acted
on, not just listed.
}
\details{
Reduction is measured on the risks common to both registers. Total RPN is the
sum of Risk Priority Numbers, mean RPN is their average, and the percentage
reduction is relative to the before total. A risk is counted as \code{downgraded}
when its risk level moves to a lower band (for example \code{critical} to \code{high}).
Risks that appear only in \code{after} are reported as \code{n_added}, and risks that
disappear are reported as \code{n_resolved}.
}
\examples{
risks <- data.frame(
  risk_id = c("R001", "R002"),
  description = c("Missing vars", "Bad derivation"),
  probability = c(4, 3), impact = c(5, 4), detectability = c(2, 3)
)
rr <- create_risk_register(risks)
rr2 <- apply_mitigations(rr, data.frame(risk_id = "R001", probability = 1))
risk_reduction_summary(rr, rr2)

}
