% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_heatmap.R
\name{plot_risk_heatmap}
\alias{plot_risk_heatmap}
\title{Plot an FMEA Risk Heatmap}
\usage{
plot_risk_heatmap(
  risk_register,
  metric = c("mean_rpn", "max_rpn", "n"),
  config = risk_config_default(),
  interactive = FALSE
)
}
\arguments{
\item{risk_register}{A \code{risk_register} from \code{\link[=create_risk_register]{create_risk_register()}}.}

\item{metric}{The cell fill, one of \code{"mean_rpn"}, \code{"max_rpn"}, or \code{"n"}.}

\item{config}{A \code{risk_config} from \code{\link[=risk_config_default]{risk_config_default()}}.}

\item{interactive}{If \code{TRUE}, return a \code{plotly} widget instead of a \code{ggplot}.
Requires the \code{plotly} package.}
}
\value{
A \code{ggplot} object, or a \code{plotly} object when \code{interactive = TRUE}.
}
\description{
Draws the standard FMEA probability by impact heatmap. Cells are coloured by
the chosen metric and labelled with the number of risks that fall in them.
}
\examples{
\dontrun{
rr <- create_risk_register(make_test_risks())
plot_risk_heatmap(rr)
}

}
