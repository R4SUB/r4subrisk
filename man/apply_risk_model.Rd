% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_models.R
\name{apply_risk_model}
\alias{apply_risk_model}
\title{Score Risks with a Model}
\usage{
apply_risk_model(risks, model = risk_model_fmea())
}
\arguments{
\item{risks}{A data.frame or \code{risk_register} with the columns the model
requires. Must include \code{risk_id}.}

\item{model}{A \code{risk_model}. Defaults to \code{\link[=risk_model_fmea]{risk_model_fmea()}}.}
}
\value{
A tibble with columns \code{risk_id}, \code{raw_score}, \code{risk_score} (0--100,
higher is more risk), \code{risk_level}, and \code{model}.
}
\description{
Applies a \code{\link[=risk_model]{risk_model()}} to a set of risks and returns per-risk raw scores,
normalized 0--100 risk scores, and qualitative levels. Any model that
produces a 0--100 risk score plugs into the same Risk pillar computation.
}
\examples{
risks <- data.frame(
  risk_id     = c("R001", "R002", "R003"),
  probability = c(4, 3, 2),
  impact      = c(5, 4, 3),
  detectability = c(2, 3, 4)
)
apply_risk_model(risks, risk_model_fmea())

}
