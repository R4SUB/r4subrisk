% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_heatmap.R
\name{risk_heatmap_data}
\alias{risk_heatmap_data}
\title{Build Risk Heatmap Data}
\usage{
risk_heatmap_data(risk_register, config = risk_config_default())
}
\arguments{
\item{risk_register}{A \code{risk_register} from \code{\link[=create_risk_register]{create_risk_register()}}.}

\item{config}{A \code{risk_config} from \code{\link[=risk_config_default]{risk_config_default()}}, used to assign a
zone to each cell from the cell's mean RPN.}
}
\value{
A tibble with one row per grid cell: \code{probability}, \code{impact}, \code{n}
(risks in the cell), \code{mean_rpn}, \code{max_rpn}, and \code{zone} (the risk level of
the cell's mean RPN, \code{NA} for empty cells).
}
\description{
Aggregates a risk register into the probability by impact grid that an FMEA
heatmap is drawn from. Every cell of the 5 by 5 grid is returned, including
empty ones, so the plotted heatmap has no gaps.
}
\examples{
rr <- create_risk_register(data.frame(
  risk_id = c("R001", "R002"),
  description = c("a", "b"),
  probability = c(4, 2), impact = c(5, 3), detectability = c(2, 3)
))
risk_heatmap_data(rr)

}
