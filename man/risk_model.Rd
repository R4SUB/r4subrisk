% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_models.R
\name{risk_model}
\alias{risk_model}
\title{Define a Risk Scoring Model}
\usage{
risk_model(name, score, max_raw, bands, required = character())
}
\arguments{
\item{name}{A short model name.}

\item{score}{A function taking a data.frame of risks and returning a numeric
vector of raw scores, one per row.}

\item{max_raw}{The raw score at maximum risk, used to normalize to 0--100.}

\item{bands}{A named list of \code{c(lower, upper)} boundaries over the raw score,
ordered from highest risk to lowest. Evaluated in order; first match wins.}

\item{required}{Character vector of columns the \code{score} function needs.}
}
\value{
An object of class \code{"risk_model"}.
}
\description{
Wraps a scoring rule so the engine can score risks with something other than
the default FMEA product. A model turns a data.frame of risks into a raw
score, a normalized 0--100 risk score, and a qualitative level. Use the
pre-built models or supply your own \code{score} function.
}
\examples{
risks <- data.frame(
  risk_id     = c("R001", "R002", "R003"),
  probability = c(4, 3, 2),
  impact      = c(5, 4, 3),
  detectability = c(2, 3, 4)
)
m <- risk_model(
  name = "impact_only",
  score = function(df) df$impact,
  max_raw = 5,
  bands = list(high = c(4, 5), medium = c(3, 3), low = c(1, 2)),
  required = "impact"
)
apply_risk_model(risks, m)

}
