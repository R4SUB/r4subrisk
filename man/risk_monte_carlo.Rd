% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_monte_carlo.R
\name{risk_monte_carlo}
\alias{risk_monte_carlo}
\title{Monte Carlo Uncertainty on Risk Priority Numbers}
\usage{
risk_monte_carlo(
  register,
  n = 10000,
  spread = 1,
  config = risk_config_default(),
  seed = NULL
)
}
\arguments{
\item{register}{A \code{risk_register} from \code{\link[=create_risk_register]{create_risk_register()}}, or a
data.frame with \code{risk_id}, \code{probability}, \code{impact}, and \code{detectability}.}

\item{n}{Number of Monte Carlo iterations. Default \code{10000}.}

\item{spread}{Standard deviation of the per-component uncertainty, in scale
points. Default \code{1}. Use \code{0} for a deterministic result.}

\item{config}{A \code{risk_config} from \code{\link[=risk_config_default]{risk_config_default()}}, used for the
critical threshold.}

\item{seed}{Optional integer seed. When supplied the random state is set for
the call and restored on exit, so the result is reproducible without
disturbing the caller's RNG stream.}
}
\value{
A list of class \code{"risk_monte_carlo"} with \code{n}, \code{spread},
\code{crit_threshold}, \code{point_total} (the deterministic register RPN),
\code{total} (named numeric summary of the total RPN distribution),
\code{total_draws} (the simulated totals, for plotting), and \code{per_risk}
(a data.frame with \code{risk_id}, \code{rpn}, \code{mc_mean}, \code{mc_sd}, \code{p05}, \code{p50},
\code{p95}, and \code{prob_critical}).
}
\description{
An FMEA scores each risk with single integer values for probability, impact,
and detectability, but those assessments are judgements with uncertainty. This
simulation treats each score as uncertain, draws many possible registers, and
reports the resulting distribution of Risk Priority Numbers, both per risk and
for the register as a whole. It turns a single RPN into a range and a
probability of being critical, which is what a risk review actually wants to
know.
}
\details{
For each draw, every FMEA component is sampled from a normal distribution
centred on its assessed value with standard deviation \code{spread} (in scale
points), then rounded and clamped to the 1--5 scale. \code{spread = 0} reproduces
the deterministic RPN. The per-draw RPN is \code{probability * impact * detectability}, and the register total is their sum. The critical threshold is
the lower bound of the top RPN band in \code{config}.
}
\examples{
risks <- data.frame(
  risk_id = c("R001", "R002", "R003"),
  description = c("Missing SDTM vars", "Unmapped derivations", "Define drift"),
  probability = c(4, 3, 2), impact = c(5, 4, 3), detectability = c(2, 3, 4)
)
rr <- suppressMessages(create_risk_register(risks))
mc <- risk_monte_carlo(rr, n = 2000, seed = 1)
mc$total
mc$per_risk

}
